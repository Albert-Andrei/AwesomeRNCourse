import React, { FC } from "react";
import { Text, View } from "react-native";

const DeckScreens: FC = () => {
  return (
    <View>
      <Text>DeckScreens</Text>
    </View>
  );
};

export default DeckScreens;
